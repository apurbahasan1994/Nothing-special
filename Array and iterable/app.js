//////// creating arrays 
const arr=[1,2,3,4,5]; // most common case

const arr2=Array(1);//

const aarr3=new Array(1); // construtor fuctions if one value is paseed than that many length array will be created with empty value

const arr4=Array.of(1);

const arr5=Array.from(1);

// splice
// adding elements between array elements
arr.splice(0,0,0);


console.log(arr)
